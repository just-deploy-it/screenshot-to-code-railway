# author: Just Ship It
# project: screenshot-to-code-railway
# purpose: Railway deployment and health-check configuration for the private API.
# used_by: Railway Backend service
# status: active
# verified: pending
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
